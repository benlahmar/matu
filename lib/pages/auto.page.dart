import 'package:flutter/material.dart';

class MyAutooffre extends StatelessWidget {
  const MyAutooffre({super.key});

  @override
  Widget build(BuildContext context) {
    return const  Center(
      child: Text("auto moto"),
    );
  }
}